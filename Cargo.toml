[package]
name = "yamux-low-mem"
version = "0.1.0"
edition = "2024"

[dependencies]
futures = "0.3"

[dev-dependencies]
tokio = { version = "1.0", features = ["full"] }
tokio-util = { version = "0.7", features = ["compat"] }
yamux = { version = "0.13.8" }
